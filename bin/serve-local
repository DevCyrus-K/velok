#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
ROUTER="$ROOT_DIR/vendor/laravel/framework/src/Illuminate/Foundation/resources/server.php"

cd "$ROOT_DIR"
rm -f public/hot
cd public

exec "$ROOT_DIR/bin/velok-php" -S 127.0.0.1:8000 "$ROUTER"
